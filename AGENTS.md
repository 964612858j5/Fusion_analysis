# Block01 项目长期规则

适用范围：本仓库所有规划、实施、复核和测试窗口。用户明确指令优先。

开始工作前必须阅读：

- `UI_SURFACE_RULES.md`：界面与交互边界。
- `docs/P0_SCOPE_RULES.md`：两次 P0 事故及永久范围约束。
- 当前任务已批准的任务块；Step1 GPU 演示任务见 `docs/step1_gpu_demo_plan.md`。

## 必须遵守

1. 任务块是封闭白名单。只实施用户已明确启动的块，文件白名单与批准的行为边界必须同时满足；同一文件内的其他功能不因此获得授权。
2. 复核结论不是实施授权。复核只验证已批准的验收标准；新增需求、状态机、威胁模型或跨模块保护只能作为 advisory 提报。
3. Blocker 必须是正常公开产品路径可复现的缺陷，或违反用户明确批准的安全边界。不得把同进程任意 Python 代码当作默认攻击者。
4. 新增或扩展 registry、authority、token、capability、journal、cache、scheduler、状态机，必须有本块明确授权。必要扩围先说明必要性、文件/模块、用户可见影响、风险和回退方式，得到用户明确批准后才执行。
5. 优先复用现有机制。局部 Intensity/UI 需求不授权修改 Viewer 读取、调度、缓存、线程、I/O 或科学算子。
6. 测试以公开产品操作和真实显示为准。受控延迟可验证已批准的正常异步行为；私有属性篡改、monkeypatch 等测试手段不构成新增产品威胁模型或重构授权。
7. 连续两轮修复未发现新的公开产品路径缺陷时，停止 hardening 并检查范围。防御机制复杂度超过功能本身时暂停，由用户裁定是否扩围或回退。
8. 科研数据、人工确认、draft/committed 和执行边界必须保护；测试只写合成项目，不得通过破坏保护机制对真实项目做变异探针。
9. 开始时核对 HEAD、分支和 git status。保护用户/其他窗口的修改及未跟踪文件，不清理、不覆盖、不整仓暂存。提交和 push 按用户授权分别执行；不得自行 reset/amend/rebase。
10. 自动测试不能替代真机验收；缓存命中、信号计数、worker 完成不等于实际像素已显示或交互流畅。

本文件是仓库持久规则入口，不代表任何新任务块已经获准启动。
